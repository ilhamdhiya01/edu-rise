import { default as MatricCard } from './MatricCard';
import { default as MyCourses } from './MyCourses';
import { default as RecomendationCourse } from './RecomendationCourse';

export const Dashboard = {
  MatricCard,
  MyCourses,
  RecomendationCourse,
} as const;
